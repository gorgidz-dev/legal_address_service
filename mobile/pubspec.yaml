name: uradres_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  dio: ^5.11.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  flutter_svg: ^2.3.0
  go_router: ^17.5.0
  google_fonts: ^8.2.1
  intl: ^0.20.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # Знак СБП для кнопки оплаты — официальные векторы НСПК (logo.zip), см.
  # lib/widgets/sbp_pay_button.dart.
  assets:
    - assets/sbp/sbp-logo-on-dark.svg
