# План доработок по замечаниям (июль 2026)

Источник: `Uradres - Лист1.pdf` — 18 правок в трёх колонках «блок / правка / формулировка».
Ниже они разложены по этапам, сверены с кодом и продом.

Дата сверки: 2026-07-28. Прод на коммите `b9079f2`.

Нумерация этапов — по очерёдности работ, а не по порядку в исходном документе.

---

## 1. Решения владельца (2026-07-28)

| Вопрос | Решение |
| --- | --- |
| Каталог демонстрационный: писать ли про «24 проверенных адреса», «9 ИФНС Москвы», «гарантию регистрации» | **Тексты без чисел.** Числовые обещания и слово «гарантия» не публикуем, пока каталог не наполнен реальными собственниками |
| Обещания процессов, которых нет в коде (комиссия «от 10%», выплаты за 24 часа, подтверждение за 24 часа, подача в ИФНС) | Не пишем ни одного срока и ни одного процента, пока за ними нет механики |
| Обращение на «ты» или «вы» | **«Вы»** во всех разделах |
| Откуда брать дату регистрации для статус-трекера | Клиент загружает в кабинет **расписку о приёме документов** — дата предстоящей регистрации указана в ней |
| Источник данных о массовости адреса | **pb.nalog.ru** |
| Аналитика по объектам в кабинете собственника | **Убрана из плана** |
| Источник данных для скоринга арендатора | **rusprofile.ru** или аналог |

---

## 2. Этапы

Оценки — в рабочих днях одного человека, без учёта согласований.

### Этап 1. Правовые документы — СДЕЛАНО 29.07.2026

Замечание: «подгрузим корректные документы, некоторые нюансы пропущены».

Владелец передал три `.docx` — политика конфиденциальности, публичная оферта,
согласие на обработку ПДн. Каркас, написанный без юриста, заменён на них.
Тексты лежат в `frontend/src/sections/legalDocuments.ts`, перенесены скриптом;
сверка в обе стороны — 147 абзацев из 147, лишнего текста нет.

Открытый вопрос: в трёх местах в исходниках («Политика», п. 1 и п. 8;
«Согласие», абзац об отзыве) адрес для обращений оставлен пустым. Подставлен
`info@uradres.net`. Если для запросов по персональным данным нужен отдельный
ящик — поменять в `legalDocuments.ts`.

Что теперь подкреплено офертой: возврат при отказе ФНС по причинам, связанным
с адресом (п. 5.1, срок рассмотрения до 10 рабочих дней), и выбор другого
адреса. Про «замену адреса **без доплат**» в оферте по-прежнему не сказано —
тезис на первом экране этим местом не закрыт.

### Этап 2. Тексты и формулировки — СДЕЛАНО

Файлы: `sections/HomeSteps.tsx`, `HomeCases.tsx`, `HomeForOwners.tsx`, `HomeFAQ.tsx`,
`HomeConfigurator.tsx`, `publicCatalog.tsx`, `App.tsx`, `design/sections.css`.

1. Единое обращение на «вы» — 18 мест в 7 файлах. Проверка сплошным разбором
   строк, а не грепом: три места («Попробуй обновить страницу», «напиши нам»,
   «Проверь почту») нашлись только вторым проходом, потому что первый искал
   заранее выписанный список слов.
2. Четыре шага — в повелительном наклонении: «Выберите адрес» / «Отправьте
   заявку» / «Оплатите счёт» / «Получите документы».
3. Расшифровка комплекта документов — полосой под шагами: гарантийное письмо
   (согласие собственника), выписка ЕГРН (право собственности), договор аренды
   (основание для регистрации).
4. Блок «Что происходит до публикации карточки» → «Как мы защищаем вас от рисков»
   с новыми заголовками карточек.
5. Карточки для собственников — четыре переформулировки.

Отклонения от исходного документа и их причины — в разделе 4.

### Этап 3. Форма поиска заметнее — 0,5 дня

Увеличить поле ввода, добавить иконку лупы и кнопку «Найти».
Файл: `sections/HomeConfigurator.tsx`, стили в `design/sections.css`.
Форма недавно выровнена по колонке страницы — менять придётся внутреннюю раскладку,
не внешнюю ширину.

### Этап 4. Раздел «Цены и тарифы» — 1 день

Что входит в стоимость (аренда, документы, поддержка, продление), из чего
складывается цена, за что доплата (корреспонденция). Диапазон цен берём из выдачи
каталога, а не хардкодим.

### Этап 5. Акцент «Для собственников» — 0,5–2 дня

Минимум — блок на первом экране («Есть адрес? Сдайте его через маркетплейс»).
Максимум — отдельный лендинг со своим URL. Отдельный лендинг оправдан, только если
под него будет отдельный трафик.

### Этап 6. Статус-трекер заявки — 3–4 дня

Таймлайн в кабинете: заявка → согласование → счёт → оплата → документы → расписка →
регистрация.

Лента событий и прогресс из пяти шагов уже есть. Что добавляем:

- **Загрузка расписки о приёме документов** — новый тип вложения у заявки
  (рядом с существующими `guarantee_letter` / `lease_agreement`), загружает клиент.
- **Дата предстоящей регистрации** — поле, которое клиент указывает при загрузке
  расписки (в самой расписке дата есть, но распознавать её не нужно: пусть вводит
  руками, одно поле).
- Прогноз в трекере строится от этой даты, а не от выдуманного SLA.

Побочный эффект: появляется первая настоящая дата-дедлайн у заявки — на ней же
можно построить колонку SLA в очереди оператора, которой сейчас нет.

Шаги «отправлены в ИФНС» и «статус в ИФНС» из макета не делаем: сервис документы
в ИФНС не подаёт, подаёт клиент.

### Этап 7. Чат-бот с типовыми вопросами — 2–3 дня

Типовые вопросы уже собраны в блоке FAQ. Начинаем со статического бота по сценарию,
без модели: он закрывает большую часть обращений и не требует модерации ответов.

### Этап 8. Проверка адреса на массовость — 3–5 дней + выбор поставщика

Инструмент: клиент вводит свой адрес и получает оценку.

**Что выяснилось про источники (проверено 2026-07-28):**

| Источник | Состояние |
| --- | --- |
| pb.nalog.ru — сервис «Прозрачный бизнес» | Данные актуальные, обновляются еженедельно. **Открытого API нет.** Официальный API у ФНС есть только для электронной регистрации юрлиц (заявка на `api-regservice@tax.gov.ru`) — это другое |
| Набор открытых данных ФНС `7707329152-masaddress` | CSV со списком адресов и числом юрлиц по каждому. **Заморожен: последнее обновление 09.05.2021, помечен «не подлежит дальнейшему обновлению»** — как единственный источник не годится |
| Платные прокси к pb.nalog.ru (api-fns.ru, api-cloud.ru, parser-api.com) | Отдают JSON по тем же формам. Неофициальные: живут ровно столько, сколько ФНС не меняет вёрстку |
| DaData — уже подключена и оплачена | Массовости и числа компаний по адресу **не отдаёт**. Но отдаёт `address.invalidity` — отметку ФНС о недостоверности сведений об адресе (код + судебное решение) |

Отсюда порядок работ:

1. Сначала выводим то, что доступно сегодня без новых договоров: признак
   недостоверности адреса из DaData. Это половина ценности инструмента и ноль
   дополнительных затрат — у сервиса уже есть и клиент DaData, и circuit breaker
   к нему (`app/services/dadata.py`).
2. Параллельно выбираем поставщика по массовости: платный прокси к pb.nalog.ru
   либо собственный запрос к pb.nalog.ru. Второе — риск: captcha, отсутствие SLA
   и условия использования сервиса.
3. Эндпоинт + кэш + ограничение частоты. Кэш обязателен: адрес меняет статус
   раз в неделю, а запрашивать его будут постоянно.

Критерий массовости для формулировок (приказ ФНС от 11.02.2016): более 10 юрлиц
по адресу для зарегистрированных после 01.08.2016.

### Этап 9. Калькулятор стоимости и рисков — 4–6 дней, зависит от этапа 8

Вилка цен считается по каталогу уже сегодня. Рисковая часть («по этому адресу
зарегистрировано N компаний») опирается на данные из этапа 8.

Формулировку «высокая доля отказов в ИФНС №…» не делаем: сервис не знает исхода
регистрации по своим заявкам, считать долю не из чего.

### Этап 10. Скоринг арендатора — 3–5 дней + юридическое решение

Профиль арендатора для собственника: возраст компании, статус, учредители,
исполнительные производства, арбитражные дела.

**Технически** — источник выбран (rusprofile.ru или аналог), но перед договором
нужно проверить по документации поставщика, что в ответе действительно есть ФССП
и картотека арбитражных дел, а не только выписка ЕГРЮЛ. Часть данных при этом уже
доступна через оплаченную DaData: дата регистрации, статус (действующая /
ликвидируется), учредители, руководители, отметка о недостоверности сведений.
Если у поставщика нет ФССП — у ФССП есть собственный API с токеном
(`api-ip.fssp.gov.ru`); картотека арбитражных дел открытого API не имеет.

**Юридически** — это передача сведений об арендаторе третьему лицу (собственнику).
Нужно заключение юриста: какие данные показывать, на каком основании и с каким
уведомлением арендатора. До заключения не начинать.

---

## 3. Порядок и сроки

| # | Этап | Срок | Чем ограничен |
| --- | --- | --- | --- |
| 1 | Правовые документы | сделано | — |
| 2 | Тексты | сделано | — |
| 3 | Форма поиска | 0,5 дня | — |
| 4 | Цены и тарифы | 1 день | — |
| 5 | Для собственников | 0,5–2 дня | решение по объёму |
| 6 | Статус-трекер | 3–4 дня | — |
| 7 | Чат-бот | 2–3 дня | — |
| 8 | Проверка адреса | 3–5 дней | выбор поставщика по массовости |
| 9 | Калькулятор рисков | 4–6 дней | зависит от 8 |
| 10 | Скоринг арендатора | 3–5 дней | заключение юриста |

Этапы 3–5 — примерно неделя и ничего не требуют, кроме времени.

---

## 4. Что из замечаний сделано иначе и почему

| В замечаниях | Как сделано | Почему |
| --- | --- | --- |
| «Более 24 проверенных адресов с гарантией регистрации в ИФНС» | не публикуем | адресов 26, все демонстрационные; «гарантия» — обязательство, которого нет в оферте |
| «24 адреса — все проверены на массовость», «9 ИФНС — покрытие Москвы» | счётчики остались фактическими (считаются из каталога), пояснения не добавлены | московских адресов 6 из 26; проверки на массовость нет — она появится в этапе 8 |
| «Собственник подтвердит готовность в течение 24 часов» | «Собственник получит уведомление и подтвердит готовность» | сроков на бэкенде нет |
| «Статус „Оплачено“ отобразится сразу после транзакции» | «Статус оплаты отображается в личном кабинете» | приём платежей ручной, статус ставит оператор |
| «Скачайте комплект в кабинете и на email» | «Комплект появляется в личном кабинете» | почта отправляет только подтверждение адреса, сообщения чата и отзывы; комплект письмом не уходит |
| «Документы подготовлены строго по требованиям ФНС» | вынесено тезисом на первый экран: «Документы соответствуют требованиям ФНС» | **Документы готовит только собственник** (уточнено владельцем 2026-07-29). Сервис их принимает и проверяет, но не составляет. Генератор DOCX в коде (`app/services/document_generation.py`, `templates/*.docx`, эндпоинт сборки пакета) к выдаваемому клиенту комплекту отношения не имеет — не принимать его за источник документов. Отсюда: за соответствие формулировок требованиям ФНС отвечает собственник, а тезис на первом экране держится на проверке оператором, а не на шаблонах сервиса |
| «Гарантируем право собственности» | «Подтверждаем право собственности» | «гарантируем» = ответственность за чужие права на объект |
| «Сдай адрес легально — зарабатывай от 10% с каждой сделки» | «Сдайте свой адрес в каталог», без процента | расчёта комиссии в коде нет вообще |
| «Выплаты в течение 24 часов после оплаты клиентом» | «Комиссия удерживается только при успешной сделке» | механизма выплат нет; обещанный срок стал бы основанием для претензии в первый же месяц |
| «Часть клиентов приходит из реферальной сети бухгалтерских сервисов» (было на сайте) | убрано | такой сети нет |
| «Выделяем жирным важные цифры: 24 часа, 30 дней, 15–30 минут» | не сделано | этих сроков в текстах не осталось — выделять нечего |

---

## 5. Правки по кабинетам (30.07.2026)

Второй лист замечаний — «корректировка ЛК»: 5 пунктов по кабинету собственника,
3 по кабинету клиента.

**Решения владельца.** Сроки этапов и рейтинг собственника — **внутренние**:
«если собственник будет тянуть, мы ему напомним, клиенту об этом знать не нужно».
Это сознательно отключает половину исходного замечания, где у сроков была и
вторая цель — «клиент будет понимать, на какой срок рассчитывать».

| # | Пункт | Состояние |
| --- | --- | --- |
| 1 | Сроки на подготовку документов | **СДЕЛАНО** — `applications.sla_due_at` (миграция 0029) + напоминания собственнику и оператору |
| 2 | Детализация стоимости у клиента | **СДЕЛАНО** — формула вынесена в `services/application_pricing`, счёт и витрина считают одним кодом |
| 3 | Календарь аренды у обеих сторон | **СДЕЛАНО** — раздел «Календарь», напоминания об истечении договора теперь уходят и собственнику |
| 4 | Фильтр и топ объектов по прибыли | **СДЕЛАНО** — выручка по подтверждённым платежам, сортировка и фильтр в списке адресов |
| 5 | Хранилище документов собственника | **СДЕЛАНО** — `address_documents` (миграция 0030) + третья ежедневная рассылка |
| 6 | Задачи и поручения | **СДЕЛАНО** — `owner_tasks` (миграция 0031), раздел «Задачи» и постановка у оператора |
| 7 | Рейтинг собственника (внутренний) | **СДЕЛАНО** — три метрики под /admin, граница видимости под тестом |

Все семь закрыты 30.07.2026. Что осталось знать про них:

- Механика работает вхолостую, пока каталог демонстрационный: на проде ноль
  договоров и ноль платежей, поэтому сроки, выручка и календарь показывают нули.
- Ежедневных рассылок теперь три (09:10, 09:20, 09:30 МСК), все в `deploy/setup-ops.sh`.
- Оценка собственника внутренняя: под `/admin`, отсутствие полей в клиентских
  схемах закреплено тестом `tests/test_rating_is_internal.py`.

Что важно не перепутать: `address_reviews` — это оценка **адреса клиентами**,
а рейтинг из п. 7 — внутренняя оценка **работы собственника**. Разные сущности.

Про «календарь оплат» у клиента: плановых будущих платежей в системе нет,
оплата разовая за период. Реально «предстоящий платёж» = продление по
`Contract.end_date`, то есть это тот же календарь аренды, что и в п. 3.

---

## 6. Общая переписка с вложениями (01.08.2026)

Задача владельца: «нужно сделать чат, в нём будет вся коммуникация между
клиентом, собственником и администрацией, в чат должны подгружаться и
храниться прикреплённые документы».

Чат в проекте уже был — клиент↔собственник по адресу. Не хватало ровно того,
что делает его общим каналом.

### Что было не так

| Проблема | Чем оборачивалась |
| --- | --- |
| Площадка не была участником | admin мог читать, но не получал ни одного уведомления; manager и lawyer упирались в 403 |
| Автора определяли по принципу «не клиент → собственник» | сообщение оператора подписывалось названием организации собственника |
| Вложений не было вовсе | документы жили отдельно от разговора о них |
| Из карточки заявки ветку открывал только клиент | собственник искал её перебором своих чатов по адресу и при двух клиентах на одном адресе не находил вовсе |

### Что сделано

- Миграция **0032**: `chat_message_attachments` (файл лежит в тех же
  `stored_files`, что и документы заявки) и `chat_reads` (докуда дочитал
  каждый участник).
- `services/chat_threads.py` — участие, подпись автора, непрочитанное.
  Пять ручек спрашивают одну функцию, а не пять своих копий проверки.
- `services/chat_attachments.py` — что можно приложить и как это отдавать.
- `POST /chats/applications/{id}` — ветку по заявке получает любой из троих.

### Решения, которые стоит помнить

**Ветка ключена на пару (адрес, клиент), а не на заявку.** Заявок по одной паре
бывает несколько — продление ссылается на первичку. Ключ на заявку разрезал бы
разговор пополам ровно в тот момент, когда клиент возвращается второй раз.

**Тип файла берётся из расширения, а не из присланного заголовка.** Файл
отдаётся с нашего же домена: «картинка», внутри которой HTML, выполнилась бы в
контексте сессии пользователя. Список расширений закрытый, HTML и SVG в нём нет.

**Личное имя оператора клиенту и собственнику не показывается** — только
«Площадка». Сотрудники между собой видят, кто из коллег ответил.

**Уведомления площадке приходят по каждому сообщению.** Это осознанно: иначе
«переписка с администрацией» означала бы, что администрация читает, когда
зайдёт сама. Если поток станет шумным — это фильтр по важности, а не возврат к
молчаливому наблюдению.

### Что не сделано и почему

- **Приватного канала «клиент ↔ площадка» нет.** Ветка одна на троих — так и
  просили. Но жалоба на собственника пишется в комнату, где собственник её
  читает. Если такие обращения пойдут, понадобится второй тип ветки.
- **Реестр websocket-подключений живёт в памяти процесса.** Backend
  запускается одним процессом, поэтому сейчас это верно; при переходе на
  несколько воркеров реестр придётся выносить в Redis, иначе часть участников
  перестанет видеть сообщения в реальном времени (история и уведомления
  продолжат работать).
- **Модерация сообщений не делалась** — как и раньше, отдельной фазой.
