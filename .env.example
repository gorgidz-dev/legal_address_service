# Скопируй в .env и заполни.
# Файл .env читается автоматически (см. app/config.py).

# --- БД ---
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/legal_address

# Пул соединений к БД (asyncpg). Дефолты подобраны для ~20-30 одновременных запросов.
# Для большой нагрузки увеличить, для маленькой VPS — уменьшить.
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20
# DB_POOL_TIMEOUT=30
# DB_POOL_RECYCLE=1800   # секунд, дропать соединения старше N
# DB_POOL_PRE_PING=true  # SELECT 1 перед checkout
# DB_ECHO=false          # включить SQL-логи (только для отладки)

# --- DaData (https://dadata.ru/profile/) ---
# Бесплатный тариф: 10 000 запросов/день. Хватает с большим запасом.
# DADATA_TOKEN=your_token_here
# DADATA_SECRET=your_secret_here

# --- S3 / MinIO для файлов выписок и сгенерированных документов ---
# STORAGE_BACKEND=local  # local или s3
# S3_ENDPOINT=https://storage.yandexcloud.net
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_BUCKET=legal-address
# S3_REGION=ru-central1

# --- Доступ ---
# SESSION_COOKIE_NAME=legal_address_session
# WEB_SESSION_TTL_HOURS=24
# MOBILE_SESSION_TTL_HOURS=168
# INVITATION_TTL_HOURS=168

# --- Cookie security (production) ---
# SESSION_COOKIE_SECURE=true              # HTTPS only
# SESSION_COOKIE_SAMESITE=lax             # lax | strict | none (none -> требует secure=true)
# SESSION_COOKIE_DOMAIN=.example.com      # для кросс-поддоменов

# --- Webhooks ---
# Общий HMAC-SHA256 секрет с платёжным провайдером — он подписывает webhook'и
# заголовком X-Webhook-Signature: sha256=<hex>. Пустое значение отключает приём.
# PAYMENT_WEBHOOK_SECRET=

# --- DaData circuit breaker ---
# DADATA_CIRCUIT_FAILURE_THRESHOLD=3
# DADATA_CIRCUIT_RECOVERY_SECONDS=30
